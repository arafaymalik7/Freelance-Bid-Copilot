{
  "name": "freelance-bid-copilot",
  "private": true,
  "version": "2.0.0",
  "description": "RAG-powered freelance bid Deal Desk",
  "scripts": {
    "dev": "node scripts/dev.js",
    "test": "npm --prefix backend test && npm --prefix frontend test",
    "build": "npm --prefix frontend run build"
  }
}
